<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GW40KArmyTracker.Pages.RosterEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="{x:Bind RosterName}" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="{x:Bind ArmyName}" FontSize="16" Opacity="0.7"/>
            <TextBlock>
                <Run Text="Points:"/>
                <Run Text="{x:Bind TotalPoints}" FontWeight="Bold"/>
                <Run Text="/"/>
                <Run Text="{x:Bind PointLimit}"/>
            </TextBlock>
        </StackPanel>

        <!-- Content: Units list and editor -->
        <Grid Grid.Row="1">
            <TextBlock Text="Roster editor coming soon..." HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>